name: CI

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Testing Github CLI
        run: gh repo list
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
